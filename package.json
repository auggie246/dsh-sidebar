{
  "name": "dsh-git-sidebar",
  "description": "Right-docked git sidebar for DSH Web: VS Code-style Source Control card (stage, commit, sync) plus a lane Commit Graph card, in modular cards owned by the right details column",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./client": "./lib/client.js",
    "./typert": "./lib/remote.js",
    "./package.json": "./package.json"
  },
  "dsh": {
    "client": {
      "inject": [
        "@deepseek-ai/dsh-client-runtime"
      ],
      "platform": "web"
    }
  },
  "files": [
    "lib/",
    "dynamic/",
    "scripts/",
    "cordis.patch.example.yml",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "bundle:dynamic": "node scripts/bundle-dynamic.mjs",
    "test": "node --check lib/client.js && node --input-type=module --check < lib/index.js && node --input-type=module --check < lib/remote.js"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/dsh-typert-protocol": "^0.1.0-rc.7",
    "react": "^18.2.0"
  }
}
