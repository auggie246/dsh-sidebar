# Bundle patch for dsh-sidebar: inserts its host row into the profile tree.
# `dsh plugin --profile web add dsh-sidebar` appends this bundle to
# dsh.profile.bundles automatically. Client roster entries come from the
# dsh.client block in package.json — no row needed here.

- insert:
    - id: sidebar
      name: 'dsh-sidebar'
