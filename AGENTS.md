# AGENTS.md

## Agent skills

### Issue tracker

GitHub Issues at `auggie246/dsh-sidebar`, accessed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage labels, each label string equal to its role name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: root `CONTEXT.md` glossary plus `docs/adr/`. See `docs/agents/domain.md`.
